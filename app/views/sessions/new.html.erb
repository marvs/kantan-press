<% content_for :title, "Sign in" %>
<% content_for :hide_site_nav, "yes" %>

<div class="auth-card">
  <h1 class="auth-card__title">Sign in</h1>

  <%= form_with url: session_path, class: "auth-form" do |form| %>
    <div class="field">
      <%= form.label :email_address, "Email address" %>
      <%= form.email_field :email_address, required: true, autofocus: true,
            autocomplete: "username", value: params[:email_address] %>
    </div>

    <div class="field">
      <%= form.label :password, "Password" %>
      <%= form.password_field :password, required: true,
            autocomplete: "current-password", maxlength: 72 %>
    </div>

    <%= form.submit "Sign in", class: "btn btn-primary auth-form__submit" %>
  <% end %>
</div>
